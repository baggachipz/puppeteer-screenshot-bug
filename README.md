Shows bug in Puppeteer (latest version) taking partial screenshots.

```
npm install
npx puppeteer browsers install chrome
npm run start
```

View the output images to compare. The one labeled 'half' is still the full image size.
